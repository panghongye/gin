# 认证
auth:
  # token有效期, 已小时为单位, 7 * 24
  duration: 168
  secretKey: secretKey

# database
db:
  type: mysql
  connStr: "root:rootroot@tcp(127.0.0.1:3306)/ghchat?charset=utf8&parseTime=True&loc=Local"
  maxIdleConn: 10
  maxOpenConn: 100

# redis
redis:
  addr: localhost:63791
  password: rootroot
  db: 0

# 业务配置
app:
